import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },
  image: {
    width: 100,
    height: 100,
    margin: 5,
    borderRadius: 20,
  },
});

export default styles;
